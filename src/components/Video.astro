---
interface VideoProps {
	id: string
	autoplay: boolean
	src: string
}
const { id, autoplay, src } = Astro.props
---

<video
	id={id}
	class='w-full object-fill rounded-lg shadow-lg'
	autoplay={autoplay}
	loop
	muted
	playsinline
>
	<source src={src} />
</video>
